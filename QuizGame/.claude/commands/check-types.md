# check-types

TypeScript 타입 오류를 검사하고 결과를 보고합니다.

## 사용법

```
/check-types
```

## 절차

1. `cd quiz-game && npx tsc --noEmit` 을 실행한다.
2. 오류가 없으면 "타입 오류 없음" 을 확인한다.
3. 오류가 있으면 각 오류를 파일·줄 번호·메시지로 요약하고 수정 방법을 제안한다.
4. 수정이 필요한 경우 사용자 확인 후 수정을 진행한다.
